doctype html
html
  head
    title Static Folder Video
    link(rel="stylesheet" href="/css/main.css")
    style.
      body {
          color: red;
        }
  body
    h1 Hello
    h3 Lorem ipsum dolor ituris arenot
    h4 Picture
    img(src="images/image.jpg")
    script(src="js/main.js")
    if !principal
      h4 REGISTRATION:
      form(action='/register' method='post')
        input(type='text' name='login')
        input(type='text' name='password')
        input(type='submit')

      h4 LOGIN:
      form(action='/login' method='post')
        input(type='text' name='login')
        input(type='text' name='password')
        input(type='submit')

      ul
        each user in users
          li 
            a(href='user/'+user.login)= user.login + ' : ' + user.password
    else
      a(href= '/logout') Logout